const money = (value, currency) => new Intl.NumberFormat("en-NG", { style: "currency", currency: currency || "NGN" }).format(Number(value || 0));
export default function SessionTotals({ session, currency, onChange }) { return <div className="border-t border-teal-100 bg-teal-50 p-4"><div className="grid gap-3 sm:grid-cols-3"><label className="text-sm font-medium text-slate-700">Discount<select value={session.discountType} onChange={(event) => onChange("discountType", event.target.value)} className="mt-1 w-full rounded-lg border border-slate-200 bg-white p-2"><option value="percentage">Percentage</option><option value="fixed">Fixed amount</option></select></label><label className="text-sm font-medium text-slate-700">Discount Value<input type="number" min="0" step="0.01" value={session.discountValue} onChange={(event) => onChange("discountValue", event.target.value)} className="mt-1 w-full rounded-lg border border-slate-200 bg-white p-2" /></label><label className="text-sm font-medium text-slate-700">VAT / Tax %<input type="number" min="0" step="0.01" value={session.taxRate} onChange={(event) => onChange("taxRate", event.target.value)} className="mt-1 w-full rounded-lg border border-slate-200 bg-white p-2" /></label></div><dl className="mt-4 grid gap-2 text-sm sm:grid-cols-2"><Row label="Session Subtotal" value={money(session.subtotal, currency)} /><Row label="Discount" value={`-${money(session.discountAmount, currency)}`} /><Row label="VAT / Tax" value={money(session.taxAmount, currency)} /><div className="flex justify-between border-t border-teal-200 pt-3 font-bold text-[#103746]"><dt>NET SESSION TOTAL</dt><dd>{money(session.netTotal, currency)}</dd></div></dl></div>; }
const Row = ({ label, value }) => <div className="flex justify-between text-slate-600"><dt>{label}</dt><dd className="font-medium text-slate-900">{value}</dd></div>;

import { CheckCircle2, FileText, Send, XCircle } from "lucide-react";
import { StatCard } from "@/components/ui";
export default function QuotationStatistics({ quotations }) { const amount = quotations.reduce((sum, quotation) => sum + Number(quotation.grandTotal || 0), 0); return <div className="grid grid-cols-1 gap-5 sm:grid-cols-2 xl:grid-cols-4"><StatCard title="Total Quotations" value={quotations.length} icon={FileText} description="All quotation records" /><StatCard title="Draft / Sent" value={`${quotations.filter((item) => item.status === "draft").length} / ${quotations.filter((item) => item.status === "sent").length}`} icon={Send} description="Awaiting customer action" accent="secondary" /><StatCard title="Accepted" value={quotations.filter((item) => item.status === "accepted").length} icon={CheckCircle2} description="Ready to convert" accent="secondary" /><StatCard title="Quotation Value" value={new Intl.NumberFormat("en-NG", { style: "currency", currency: "NGN", maximumFractionDigits: 0 }).format(amount)} icon={XCircle} description={`${quotations.filter((item) => item.status === "rejected").length} rejected`} accent="accent" /></div>; }

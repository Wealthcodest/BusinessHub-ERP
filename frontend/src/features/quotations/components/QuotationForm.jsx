import {useEffect,useRef,useState}from"react";import{useForm}from"react-hook-form";import{zodResolver}from"@hookform/resolvers/zod";import{Button,Card,EmptyState,Input,Textarea}from"@/components/ui";import PaymentScheduleEditor from"@/components/documents/PaymentScheduleEditor";import{SessionContainer,createDocumentSession,useDocumentSessions}from"@/features/documentSessions";import{ThemeSelector}from"@/features/documentThemes";import{quotationSchema}from"../schemas/quotationSchema";import{calculateQuotation}from"../utils/quotationCalculator";import QuotationTotalsCard from"./QuotationTotalsCard";
const Field=({label,error,children})=><label className="mb-3 block text-sm font-medium text-slate-700">{label}{children}{error&&<span className="mt-1 block text-xs text-rose-600">{error.message}</span>}</label>;
export default function QuotationForm({businesses,customers,products,defaultValues={},loading,onSubmit,onCreateBusiness}){const initial=defaultValues.sessions?.length?defaultValues.sessions:[createDocumentSession()],controller=useDocumentSessions(initial,products),[saving,setSaving]=useState(false),previous=useRef("");const{register,handleSubmit,watch,setValue,formState:{errors}}=useForm({resolver:zodResolver(quotationSchema),defaultValues:{businessId:businesses[0]?.id||"",customerId:customers[0]?.id||"",themeId:"",quotationNumber:"",issueDate:new Date().toISOString().slice(0,10),expiryDate:new Date(Date.now()+30*86400000).toISOString().slice(0,10),status:"draft",currency:"NGN",notes:"",terms:"",paymentSchedule:undefined,sessions:initial,...defaultValues}});useEffect(()=>{const next=JSON.stringify(controller.sessions);if(previous.current!==next){previous.current=next;setValue("sessions",controller.sessions,{shouldDirty:true});}},[controller.sessions,setValue]);const currency=watch("currency"),businessId=watch("businessId"),themeId=watch("themeId"),paymentSchedule=watch("paymentSchedule"),totals=calculateQuotation(controller.sessions);if(!businesses.length)return <EmptyState title="Create a Business First" description="Quotations must be linked to a business." primaryAction={{label:"Create Business",onClick:onCreateBusiness}}/>;if(!customers.length)return <EmptyState title="Create a Customer First" description="Quotations need a customer before they can be created." primaryAction={{label:"Create Customer",onClick:()=>window.location.assign("/customers/new")}}/>;async function submit(data){setSaving(true);try{await onSubmit({...data,...totals,sessions:data.sessions||controller.sessions});}finally{setSaving(false);}}return <form onSubmit={handleSubmit(submit)} className="space-y-6"><Card><h2 className="mb-5 text-lg font-semibold">Quotation Information</h2><div className="grid gap-4 md:grid-cols-2"><Field label="Business *" error={errors.businessId}><select {...register("businessId")} className="mt-1 w-full rounded-lg border border-slate-200 p-2.5">{businesses.map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select></Field><Field label="Customer *" error={errors.customerId}><select {...register("customerId")} className="mt-1 w-full rounded-lg border border-slate-200 p-2.5">{customers.map(c=><option key={c.id} value={c.id}>{c.displayName}</option>)}</select></Field><ThemeSelector businessId={businessId} value={themeId} onChange={v=>setValue("themeId",v,{shouldDirty:true})}/><Field label="Quotation Number"><Input {...register("quotationNumber")}/></Field><Field label="Status"><select {...register("status")} className="mt-1 w-full rounded-lg border border-slate-200 p-2.5">{["draft","sent","accepted","rejected","expired"].map(s=><option key={s}>{s}</option>)}</select></Field><Field label="Issue Date"><Input type="date" {...register("issueDate")}/></Field><Field label="Expiry Date"><Input type="date" {...register("expiryDate")}/></Field></div></Card><SessionContainer controller={controller} currency={currency}/><div className="grid gap-6 lg:grid-cols-2"><Card><Field label="Currency"><select {...register("currency")} className="mt-1 w-full rounded-lg border border-slate-200 p-2.5"><option>NGN</option><option>USD</option><option>GBP</option></select></Field></Card><QuotationTotalsCard totals={totals} currency={currency}/></div><PaymentScheduleEditor value={paymentSchedule} onChange={v=>setValue("paymentSchedule",v,{shouldDirty:true,shouldValidate:true})} grandTotal={totals.grandTotal} currency={currency}/>{errors.paymentSchedule&&<p className="text-sm text-rose-600">{errors.paymentSchedule.message}</p>}<Card><div className="grid gap-4 md:grid-cols-2"><Field label="Notes"><Textarea rows={4} {...register("notes")}/></Field><Field label="Terms & Conditions"><Textarea rows={4} {...register("terms")}/></Field></div></Card><div className="flex justify-end"><Button type="submit" disabled={loading||saving}>{loading||saving?"Saving...":"Save Quotation"}</Button></div></form>;}

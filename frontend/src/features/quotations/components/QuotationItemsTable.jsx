import { Plus, Trash2 } from "lucide-react";
import { Button, Input } from "@/components/ui";
export default function QuotationItemsTable({ fields, products, register, setValue, remove, append, watch }) { function selectProduct(index, productId) { const product = products.find((item) => String(item.id) === productId); setValue(`items.${index}.productId`, productId); if (product) { setValue(`items.${index}.description`, product.name); setValue(`items.${index}.unitPrice`, product.sellingPrice); setValue(`items.${index}.tax`, product.taxRate || 0); } } return <div className="overflow-x-auto rounded-xl border border-slate-200"><table className="w-full min-w-[850px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr>{["Product / Service", "Description", "Qty", "Unit Price", "Discount %", "Tax %", "Total", ""].map((label) => <th key={label} className="px-3 py-3 font-semibold">{label}</th>)}</tr></thead><tbody>{fields.map((field, index) => { const quantity = Number(watch(`items.${index}.quantity`) || 0); const unitPrice = Number(watch(`items.${index}.unitPrice`) || 0); const discount = Number(watch(`items.${index}.discount`) || 0); const tax = Number(watch(`items.${index}.tax`) || 0); const total = quantity * unitPrice * (1 - discount / 100) * (1 + tax / 100); return <tr key={field.id} className="border-t border-slate-100"><td className="p-3"><select value={watch(`items.${index}.productId`) || ""} onChange={(event) => selectProduct(index, event.target.value)} className="w-40 rounded-lg border border-slate-200 p-2"><option value="">Manual item</option>{products.map((product) => <option key={product.id} value={product.id}>{product.name}</option>)}</select></td><td className="p-3"><Input {...register(`items.${index}.description`)} className="min-w-44" /></td><td className="p-3"><Input type="number" min="1" {...register(`items.${index}.quantity`)} className="w-20" /></td><td className="p-3"><Input type="number" min="0" step="0.01" {...register(`items.${index}.unitPrice`)} className="w-28" /></td><td className="p-3"><Input type="number" min="0" step="0.01" {...register(`items.${index}.discount`)} className="w-20" /></td><td className="p-3"><Input type="number" min="0" step="0.01" {...register(`items.${index}.tax`)} className="w-20" /></td><td className="p-3 font-semibold text-slate-700">{total.toLocaleString(undefined, { maximumFractionDigits: 2 })}</td><td className="p-3"><button type="button" aria-label="Remove item" onClick={() => remove(index)} className="rounded-lg p-2 text-rose-600 hover:bg-rose-50"><Trash2 className="h-4 w-4" /></button></td></tr>; })}</tbody></table><div className="border-t border-slate-100 p-3"><Button type="button" variant="secondary" onClick={() => append({ id: `item-${Date.now()}`, productId: "", description: "", quantity: 1, unitPrice: 0, discount: 0, tax: 0 })}><Plus className="mr-2 h-4 w-4" />Add Line Item</Button></div></div>; }

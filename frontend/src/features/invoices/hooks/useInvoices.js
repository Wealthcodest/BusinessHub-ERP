import { useCallback, useEffect, useState } from "react"; import { invoiceService } from "../services/invoiceService"; import { useActiveBusiness } from "@/app/ActiveBusinessContext";
export default function useInvoices() { const { activeBusinessId } = useActiveBusiness(); const [invoices, setInvoices] = useState([]), [loading, setLoading] = useState(true); const refreshInvoices = useCallback(async () => { setLoading(true); try { setInvoices((await invoiceService.getAll()).filter((item) => !activeBusinessId || String(item.businessId) === activeBusinessId)); } finally { setLoading(false); } }, [activeBusinessId]); useEffect(() => { refreshInvoices(); }, [refreshInvoices]); return { invoices, loading, refreshInvoices }; }

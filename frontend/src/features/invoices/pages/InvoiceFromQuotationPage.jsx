import {useEffect} from "react";import {useNavigate,useParams} from "react-router-dom";import {quotationService} from "@/features/quotations/services/quotationService";import {invoiceService} from "../services/invoiceService";export default function InvoiceFromQuotationPage(){const{id}=useParams(),n=useNavigate();useEffect(()=>{quotationService.getById(id).then(async q=>{if(q?.status==="accepted"){const i=await invoiceService.fromQuotation(q);n(`/invoices/${i.id}/edit`)}else n(`/quotations/${id}`)})},[id,n]);return null}

import {Card} from "@/components/ui"; const money=(v,c)=>new Intl.NumberFormat("en-NG",{style:"currency",currency:c||"NGN"}).format(Number(v||0)); export default function InvoiceTotalsCard({invoice}){return <Card><h2 className="text-lg font-semibold">Invoice Totals</h2><div className="mt-4 space-y-2 text-sm"><p className="flex justify-between"><span>Subtotal</span><b>{money(invoice.subtotal,invoice.currency)}</b></p><p className="flex justify-between"><span>Discount</span><b>-{money(invoice.discountAmount,invoice.currency)}</b></p><p className="flex justify-between"><span>Tax</span><b>{money(invoice.taxAmount,invoice.currency)}</b></p><p className="flex justify-between border-t pt-3 text-base text-[#103746]"><span>Grand Total</span><b>{money(invoice.grandTotal,invoice.currency)}</b></p></div></Card>}

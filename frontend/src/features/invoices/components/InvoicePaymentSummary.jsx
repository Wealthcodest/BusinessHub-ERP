import {Card} from "@/components/ui"; export default function InvoicePaymentSummary({invoice}){const f=v=>new Intl.NumberFormat("en-NG",{style:"currency",currency:invoice.currency||"NGN"}).format(Number(v||0));return <Card><h2 className="text-lg font-semibold">Payment Summary</h2><div className="mt-4 space-y-2 text-sm"><p className="flex justify-between"><span>Amount Paid</span><b>{f(invoice.amountPaid)}</b></p><p className="flex justify-between text-[#103746]"><span>Outstanding Balance</span><b>{f(invoice.balance)}</b></p><p>Payment Terms: {invoice.paymentTerms||"—"}</p></div></Card>}

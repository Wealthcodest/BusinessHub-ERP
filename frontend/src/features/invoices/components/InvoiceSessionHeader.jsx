import { Input, Textarea } from "@/components/ui";
import SessionToolbar from "./SessionToolbar";
export default function InvoiceSessionHeader({ session, index, count, collapsed, onChange, ...toolbar }) { return <div className="flex gap-3 border-b border-teal-100 bg-teal-50/70 p-4"><div className="min-w-0 flex-1"><div className="flex items-center gap-3"><span className="rounded bg-[#103746] px-2 py-1 text-xs font-bold uppercase tracking-wide text-white">Session {index + 1}</span><Input aria-label="Session title" value={session.title} onChange={(event) => onChange("title", event.target.value)} className="max-w-md border-0 bg-transparent px-0 text-lg font-semibold text-[#103746] focus:ring-0" /></div>{!collapsed && <Textarea aria-label="Session description" rows={2} value={session.description || ""} onChange={(event) => onChange("description", event.target.value)} placeholder="Description (optional)" className="mt-3 max-w-2xl bg-white text-sm" />}</div><SessionToolbar collapsed={collapsed} canMoveUp={index > 0} canMoveDown={index < count - 1} {...toolbar} /></div>; }

export default function InvoiceSessionSubtotal({ subtotal, currency }) { const amount = new Intl.NumberFormat("en-NG", { style: "currency", currency: currency || "NGN" }).format(Number(subtotal || 0)); return <div className="flex items-center justify-between border-t border-teal-100 bg-teal-50 px-5 py-4"><span className="text-xs font-bold uppercase tracking-wider text-[#103746]">Session subtotal</span><strong className="text-lg text-[#103746]">{amount}</strong></div>; }

import { Plus, Trash2 } from "lucide-react";
import { Button, Input } from "@/components/ui";
const fields = [["description", "Description", "text"], ["quantity", "Qty", "number"], ["unit", "Unit", "text"], ["unitPrice", "Price", "number"], ["discount", "Disc. %", "number"], ["tax", "Tax %", "number"]];
export default function InvoiceSessionItems({ items, currency, onChange, onRemove, onAdd }) { const money = (value) => new Intl.NumberFormat("en-NG", { style: "currency", currency: currency || "NGN" }).format(Number(value || 0)); return <div className="overflow-x-auto p-4"><table className="min-w-[820px] w-full text-left text-sm"><thead className="border-b border-slate-200 text-xs uppercase tracking-wide text-slate-500"><tr>{fields.map(([, label]) => <th key={label} className="px-2 py-2">{label}</th>)}<th className="px-2 py-2 text-right">Amount</th><th /></tr></thead><tbody>{items.map((item, index) => <tr key={item.id} className="border-b border-slate-100">{fields.map(([key, label, type]) => <td key={key} className="p-2"><Input aria-label={`${label} item ${index + 1}`} type={type} min={type === "number" ? "0" : undefined} step={key === "quantity" ? "1" : "0.01"} value={item[key] ?? ""} onChange={(event) => onChange(index, key, event.target.value)} /></td>)}<td className="px-2 text-right font-semibold text-[#103746]">{money(item.lineTotal)}</td><td className="p-2"><button type="button" aria-label="Remove item" onClick={() => onRemove(index)} className="rounded p-2 text-rose-600 hover:bg-rose-50"><Trash2 className="h-4 w-4" /></button></td></tr>)}</tbody></table><Button type="button" onClick={onAdd} className="mt-4"><Plus className="mr-2 inline h-4 w-4" />Add Item</Button></div>; }

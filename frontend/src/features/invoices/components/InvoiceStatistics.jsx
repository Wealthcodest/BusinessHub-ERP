import {FileText,CheckCircle2,CircleDollarSign,TriangleAlert} from "lucide-react";import {StatCard} from "@/components/ui";export default function InvoiceStatistics({invoices}){const rev=invoices.reduce((s,i)=>s+Number(i.grandTotal||0),0);return <div className="grid grid-cols-1 gap-5 sm:grid-cols-2 xl:grid-cols-4"><StatCard title="Total Invoices" value={invoices.length} icon={FileText} description="All invoice records"/><StatCard title="Paid" value={invoices.filter(i=>i.status==="paid").length} icon={CheckCircle2} description="Settled invoices" accent="secondary"/><StatCard title="Outstanding" value={invoices.filter(i=>Number(i.balance)>0).length} icon={CircleDollarSign} description="Balances due" accent="accent"/><StatCard title="Revenue" value={new Intl.NumberFormat("en-NG",{style:"currency",currency:"NGN",maximumFractionDigits:0}).format(rev)} icon={TriangleAlert} description={`${invoices.filter(i=>i.status==="overdue").length} overdue"`} accent="secondary"/></div>}

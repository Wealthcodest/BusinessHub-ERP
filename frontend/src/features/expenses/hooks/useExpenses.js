import { useCallback, useEffect, useState } from "react"; import { expenseService } from "../services/expenseService"; import { useActiveBusiness } from "@/app/ActiveBusinessContext";
export default function useExpenses() { const { activeBusinessId } = useActiveBusiness(); const [expenses, setExpenses] = useState([]); const [loading, setLoading] = useState(true); const refreshExpenses = useCallback(async () => { setLoading(true); try { setExpenses((await expenseService.getAll()).filter((item) => !activeBusinessId || String(item.businessId) === activeBusinessId)); } finally { setLoading(false); } }, [activeBusinessId]); useEffect(() => { refreshExpenses(); }, [refreshExpenses]); return { expenses, loading, refreshExpenses }; }

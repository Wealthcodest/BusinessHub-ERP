import { Card } from "@/components/ui";
import ProductStatusBadge from "./ProductStatusBadge";
export default function ProductInventoryCard({ product }) { if (product.type === "Service") return null; return <Card><div className="flex items-center justify-between"><h2 className="text-lg font-semibold text-slate-900">Inventory</h2><ProductStatusBadge product={product} /></div><dl className="mt-5 space-y-4 text-sm"><Row label="Track Inventory" value={product.trackInventory ? "Yes" : "No"} /><Row label="Opening Stock" value={`${product.openingStock || 0} ${product.unit || ""}`} /><Row label="Minimum Stock" value={`${product.minimumStock || 0} ${product.unit || ""}`} /><Row label="Reorder Level" value={`${product.reorderLevel || 0} ${product.unit || ""}`} /></dl></Card>; }
const Row = ({ label, value }) => <div className="flex items-center justify-between border-b border-slate-100 pb-3 last:border-0"><dt className="text-slate-500">{label}</dt><dd className="font-semibold text-slate-800">{value}</dd></div>;

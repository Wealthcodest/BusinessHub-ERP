import { Card } from "@/components/ui";
const money = (value, currency) => new Intl.NumberFormat("en-NG", { style: "currency", currency: currency || "NGN", maximumFractionDigits: 2 }).format(Number(value || 0));
export default function ProductPriceCard({ product }) { const cost = Number(product.costPrice || 0); const sale = Number(product.sellingPrice || 0); const profit = sale - cost; const margin = sale ? (profit / sale) * 100 : 0; const markup = cost ? (profit / cost) * 100 : 0; return <Card><h2 className="text-lg font-semibold text-slate-900">Pricing</h2><dl className="mt-5 space-y-4 text-sm"><Row label="Cost Price" value={money(cost, product.currency)} /><Row label="Selling Price" value={money(sale, product.currency)} /><Row label="Profit" value={money(profit, product.currency)} /><Row label="Margin" value={`${margin.toFixed(1)}%`} /><Row label="Markup" value={`${markup.toFixed(1)}%`} /><Row label="Tax Rate" value={`${product.taxRate || 0}%`} /></dl></Card>; }
const Row = ({ label, value }) => <div className="flex items-center justify-between border-b border-slate-100 pb-3 last:border-0"><dt className="text-slate-500">{label}</dt><dd className="font-semibold text-slate-800">{value}</dd></div>;

export default function ProductStatusBadge({ product }) { const inventory = product.type === "Product" && product.trackInventory; const stock = Number(product.openingStock || 0); const low = stock <= Number(product.reorderLevel || product.minimumStock || 0); const label = product.status !== "active" ? "Inactive" : !inventory ? "Service" : stock === 0 ? "Out of Stock" : low ? "Low Stock" : "In Stock"; const colors = { "In Stock": "bg-emerald-50 text-emerald-700", "Low Stock": "bg-amber-50 text-amber-700", "Out of Stock": "bg-rose-50 text-rose-700", Inactive: "bg-slate-100 text-slate-600", Service: "bg-sky-50 text-sky-700" }; return <span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-semibold ${colors[label]}`}>{label}</span>; }

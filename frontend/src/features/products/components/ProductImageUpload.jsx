import { ImagePlus, X } from "lucide-react";
export default function ProductImageUpload({ label, preview, onChange, onRemove, multiple = false }) { return <div><p className="text-sm font-medium text-slate-700">{label}</p><label className="mt-2 flex cursor-pointer items-center justify-center rounded-xl border border-dashed border-slate-300 bg-slate-50 p-4 text-sm text-slate-500 hover:border-[#103746]"><ImagePlus className="mr-2 h-4 w-4" />Choose image{multiple ? "s" : ""}<input type="file" accept="image/*" multiple={multiple} className="sr-only" onChange={onChange} /></label>{preview && <div className="relative mt-3 inline-block"><img src={preview} alt={`${label} preview`} className="h-24 w-24 rounded-lg object-cover" /><button type="button" aria-label={`Remove ${label}`} onClick={onRemove} className="absolute -right-2 -top-2 rounded-full bg-slate-800 p-1 text-white"><X className="h-3 w-3" /></button></div>}</div>; }

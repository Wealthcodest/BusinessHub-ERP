import { ImageOff, X } from "lucide-react";
export default function ProductGallery({ product, editable = false, onRemove }) { const images = [product.thumbnail, ...(product.images || [])].filter(Boolean); if (!images.length) return <div className="flex aspect-square items-center justify-center rounded-xl bg-slate-100 text-slate-400"><ImageOff className="h-10 w-10" /></div>; return <div className="grid grid-cols-2 gap-3 sm:grid-cols-3">{images.map((image, index) => <div key={`${image}-${index}`} className="relative"><img src={image} alt={`${product.name} ${index + 1}`} className="aspect-square w-full rounded-xl object-cover" />{editable && <button type="button" onClick={() => onRemove(index)} aria-label="Remove image" className="absolute right-2 top-2 rounded-full bg-slate-900/80 p-1 text-white"><X className="h-3 w-3" /></button>}</div>)}</div>; }

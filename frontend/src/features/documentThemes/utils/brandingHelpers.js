export const getBusinessInitials = (name = "Business") => name.split(/\s+/).filter(Boolean).slice(0, 3).map((part) => part[0]).join("").toUpperCase() || "BH";
export const getLogoSize = (branding = {}) => ({ small: 60, medium: 90, large: 130, custom: Math.max(40, Math.min(200, Number(branding.customLogoSize) || 90)) })[branding.logoSize] || 90;
export const getLogoPositionClass = (position) => ({ left: "items-start text-left", center: "items-center text-center", right: "items-end text-right" })[position] || "items-start text-left";
export const getBusinessContactLines = (business = {}, branding = {}) => [branding.showBusinessAddress && [business.address, business.city, business.state, business.country].filter(Boolean).join(", "), branding.showBusinessPhone && business.phone, branding.showBusinessEmail && business.email, branding.showBusinessWebsite && business.website, branding.showRegistrationNumber && business.registrationNumber && `Reg: ${business.registrationNumber}`, branding.showTaxNumber && business.taxNumber && `Tax: ${business.taxNumber}`].filter(Boolean);

export default function DocumentTotals({ totals, currency, config }) { const money = (value) => new Intl.NumberFormat("en-NG", { style: "currency", currency: currency || "NGN" }).format(Number(value || 0)); const filled = config.grandTotalEmphasis === "accent" ? "var(--document-accent)" : "var(--document-total-background)"; return <div className="ml-auto max-w-xs space-y-2 p-4 text-right" style={{ backgroundColor: "var(--document-total-background)", color: "var(--document-total-text)" }}>{config.showSubtotal && <p>Subtotal: <b>{money(totals.subtotal)}</b></p>}{config.showDiscount && <p>Discount: <b>-{money(totals.discountAmount)}</b></p>}{config.showTax && <p>Tax: <b>{money(totals.taxAmount)}</b></p>}{config.showGrandTotal && <p className="border-t pt-3" style={{ borderColor: "var(--document-border)", backgroundColor: config.grandTotalEmphasis === "filled" || config.grandTotalEmphasis === "accent" ? filled : "transparent", fontWeight: config.grandTotalEmphasis === "strong" || config.grandTotalEmphasis === "emphasized" ? 700 : 400 }}>Grand Total: {money(totals.grandTotal)}</p>}</div>; }

import { StatCard } from "@/components/ui";
import { UserCheck, UserMinus, Users } from "lucide-react";
export default function CustomerStatistics({ customers }) { const active = customers.filter((customer) => customer.status === "active").length; const inactive = customers.filter((customer) => customer.status === "inactive").length; const month = new Date().toISOString().slice(0, 7); const newThisMonth = customers.filter((customer) => customer.createdAt?.startsWith(month)).length; return <div className="grid grid-cols-1 gap-5 sm:grid-cols-2 xl:grid-cols-4"><StatCard title="Total Customers" value={customers.length} icon={Users} description="Across all businesses" /><StatCard title="Active Customers" value={active} icon={UserCheck} description="Ready to transact" accent="secondary" /><StatCard title="Inactive Customers" value={inactive} icon={UserMinus} description="Require attention" accent="accent" /><StatCard title="New This Month" value={newThisMonth} icon={Users} description="Recently added" accent="secondary" /></div>; }

import { Eye, Pencil, Trash2, Users } from "lucide-react";
import { DataTable } from "@/components/ui";
import CustomerAvatar from "./CustomerAvatar";
import CustomerStatusBadge from "./CustomerStatusBadge";
const columns = [{ key: "displayName", label: "Customer", sortable: true, render: (customer) => <div className="flex items-center gap-3"><CustomerAvatar customer={customer} size="sm" /><div><p className="font-medium text-slate-800">{customer.displayName}</p><p className="text-xs text-slate-500">{customer.companyName || customer.customerCode}</p></div></div> }, { key: "email", label: "Email", sortable: true }, { key: "phone", label: "Phone", sortable: true }, { key: "status", label: "Status", sortable: true, render: (customer) => <CustomerStatusBadge status={customer.status} /> }, { key: "createdAt", label: "Created", sortable: true }];
export default function CustomerTable({ customers, loading, onCreate, onView, onEdit, onDelete }) { return <DataTable data={customers} columns={columns} loading={loading} getRowLabel={(customer) => customer.displayName} searchFields={["firstName", "lastName", "companyName", "displayName", "email", "phone"]} searchPlaceholder="Search customers..." primaryAction={{ label: "+ New Customer", onClick: onCreate }} emptyState={{ icon: Users, title: "No customers found", description: "Create your first customer.", primaryAction: { label: "+ New Customer", onClick: onCreate } }} getRowActions={(customer) => [{ label: "View", icon: Eye, onClick: () => onView(customer) }, { label: "Edit", icon: Pencil, onClick: () => onEdit(customer) }, { label: "Delete", icon: Trash2, destructive: true, onClick: () => onDelete(customer) }]} />; }

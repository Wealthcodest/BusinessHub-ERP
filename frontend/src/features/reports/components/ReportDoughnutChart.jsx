const palette = ["#18566E", "#14B8A6", "#D7B159", "#F97316", "#E11D48", "#8B5CF6", "#64748B"];

export default function ReportDoughnutChart({ title, description, data, formatMoney, centerLabel = "total" }) {
  const rows = data.filter((item) => Number(item.value) > 0);
  const total = rows.reduce((sum, item) => sum + Number(item.value || 0), 0);
  const gradient = total ? `conic-gradient(${rows.map((item, index) => { const previous = rows.slice(0, index).reduce((sum, entry) => sum + Number(entry.value || 0), 0); const current = previous + Number(item.value || 0); return `${item.color || palette[index % palette.length]} ${(previous / total) * 100}% ${(current / total) * 100}%`; }).join(", ")})` : "#e2e8f0";
  return <section className="rounded-lg border border-slate-200 bg-white p-5 shadow-[0_1px_2px_rgb(15_23_42/0.04)]"><h2 className="font-semibold text-slate-900">{title}</h2><p className="mt-1 text-sm text-slate-500">{description}</p>{total ? <div className="mt-6 flex flex-col items-center gap-5 sm:flex-row"><div className="grid h-40 w-40 shrink-0 place-items-center rounded-full" style={{ background: gradient }}><div className="grid h-24 w-24 place-items-center rounded-full bg-white px-2 text-center"><b className="max-w-full truncate text-sm text-slate-800">{formatMoney ? formatMoney(total) : total}</b><span className="text-[10px] uppercase tracking-wide text-slate-500">{centerLabel}</span></div></div><div className="w-full space-y-2.5">{rows.map((item, index) => <div key={item.label} className="flex items-center justify-between gap-3 text-sm"><span className="flex min-w-0 items-center gap-2 text-slate-600"><i className="h-2.5 w-2.5 shrink-0 rounded-full" style={{ backgroundColor: item.color || palette[index % palette.length] }} /> <span className="truncate">{item.label}</span></span><span className="shrink-0 font-semibold text-slate-800">{formatMoney ? formatMoney(item.value) : item.value}</span></div>)}</div></div> : <p className="py-16 text-center text-sm text-slate-500">No data available for this period.</p>}</section>;
}
